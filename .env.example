# Volcengine Seedream API Key (server-side only)
SEEDREAM_API_KEY="your_seedream_api_key"

# Supabase Configuration (from supabase.com > Settings > API)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your_supabase_anon_key"

# API URL (leave empty for same-origin in production, set for local dev if needed)
VITE_API_URL=""
